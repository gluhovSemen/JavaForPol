1.Написать приложение которое вводит 3 целых положительных числа  из командной строки и находит наибольший общий делитель.

2.  Создать приложение с 2 параметрами. В качестве параметров задаются  любые  неотрицательные целые числа как число строк и столбцов матрицы.
 1-ое число - число строк, 2-ое число- число столбцов. Элементом матрицы является число состоящее из номера строки и номера столбца. 
Вывести на дисплей заданную матрицу .

3. Есть класс Creature c подклассами Snake, Dog  и есть интерфейс Creep c методом creep, а также унаследованный от Creep  подинтерфейс Wriggle c методом wriggle.
 Класс Snake реализует интерфейс Wriggle, а класс Dog - Creep.
  Создать массив объектов Сreep, как представителей классов Snake , Dog, состоящий из количества  элементов, заданных параметром.
 Для всех  этих объектов выполнить все  методы, которые реализованы в соответствующих классах.

4. Создать 2 потока, один из которых записывает любое число  в разделенную между потоками переменную , а другой считывает это число.
 Цикл для каждого потока выполняется 100 раз.  
Нужно выводить имя работающего потка и - записываемое число   для первого потока или считанное число для второго потока  . Конструкцию synchronized не использовать в этом задании.

5. Выполнить задание 4  с использованием конструкции synchronized . Не использовать в этом задании флаги для синхронизации потоков, а только методы wait и notify. 
Также не использовать любые задержки для потоков после начала их работы в виде методов sleep, yield или wait c параметром.

6. 3 потока выполняют следующую работу: 1-ый генерирует произвольное целое  число , 2-ой поток ставит в соответствие этому числу соответствующую букву английского алфавита( по модулю 26),
 3-ий поток собирает слова из букв, слово заканчивается , когда  сгенерирована гласная буква "e". Работа потоков заканчивается после того как выведено количество слов,заданное параметром. 
Использовать ограничения из задания 5. 
Выводить на дисплей результаты работы каждого потока.

7. Создать приложение с параметрами.
 параметр 1- имя файла, параметр 2 -имя выходного файла, параметр 3 -строка( например- Exception).
Вывести в файл, заданный 2 параметром,  строки из файла, заданного 1 параметром, которые содержат строку, заданную 3 параметром.
Строки, которые выводятся в файл , должны быть уникальными (неповторяющимися), на консоль вывести все найденные строки, их количество , а также количество уникальных строк.

8. Создать приложение для мобильного телефона , которое выводит текст, заданный с помощью интерфейса, на экран . Также с помощью интерфейса нужно выбрать шрифт для вывода текста   .

9. Создать приложение для мобильного телефона, строящее график вида y=ax+b, где а и b задаются через интерфейс, в приложении нужно вывести  оси координат с подписями.

10. Создать приложение-игру для мобильного телефона.